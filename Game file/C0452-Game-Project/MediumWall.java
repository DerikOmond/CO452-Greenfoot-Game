import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class MediumWall here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class MediumWall extends Wall
{
    public MediumWall()
    {
        getImage().scale(getImage().getWidth() * 3, getImage().getWidth());    
    }
    
    /**
     * Act - do whatever the MediumWall wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act()
    {
        // Add your action code here.
    }
}
